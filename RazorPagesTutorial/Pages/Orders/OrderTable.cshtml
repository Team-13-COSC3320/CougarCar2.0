@page
@model RazorPagesTutorial.OrderTableModel
@{
    ViewData["Title"] = "OrderTalbe";
}

<h1>Order Table</h1>



<table class="table">
    <thead>
        <tr>
            <th>

            </th>
            <th>
                @Html.DisplayNameFor(model => model.Orders[0].O_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Orders[0].O_Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Orders[0].O_UID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Orders[0].O_PIDS)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Orders[0].O_Status)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Orders)
        {
            <tr>
                <td>
                    <div id="Edit">
                        <a class="btn btn-large btn-info" asp-page="/Orders/OrderEdit" asp-route-ID="@item.O_ID">Edit</a>
                    </div>
                    @*<div id="Delete">
                        <a class="btn btn-large btn-danger" asp-page="/Reviews/ReviewDelete" asp-route-ID="@item.R_ID">Delete</a>
                    </div>*@
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.O_ID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.O_Date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.O_UID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.O_PIDS)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.O_Status)
                </td>
            </tr>
        }
    </tbody>
</table>


